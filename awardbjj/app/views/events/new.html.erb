<main class="mx-auto my-10 md:w-2/3 w-full">
	<%= link_to sanitize("&larr; Back to profile"), user_path(current_user), class: "text-blue-500 hover:underline mb-4 inline-block" %>

	<h1 class="font-bold text-4xl">New Event</h1>

	<%= render "form", event: @event %>
</main>