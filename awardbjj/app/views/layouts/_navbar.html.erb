<nav class="bg-gray-800 p-4">
  <div class="mx-auto flex justify-between items-center">
    <!-- Left Side -->
    <%= link_to "Award BJJ", root_path, class: "text-white text-lg font-bold" %>
    <%#= TODO: navbar is different for main page and for login/signup page %>
    
    <!-- Right Side -->
    <div class="flex items-center space-x-4">
      <%= link_to "Events", events_path, class: "text-white" %>
      <% if user_signed_in? %>
        <%= link_to current_user.full_name, user_path(current_user) , class: "text-white"%>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-white text-gray-800 py-2 px-4 rounded-full hover:bg-gray-200 transition duration-300" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "text-white" %>
        <%= link_to "Sign up", new_user_registration_path, class: "bg-white text-gray-800 py-2 px-4 rounded-full hover:bg-gray-200 transition duration-300" %>
      <% end %>
    </div>
  </div>
</nav>
